# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Environment
NODE_ENV=production 

# ============================================================================
# Azure AD Authentication (Optional - for Fabric Data Agent identity passthrough)
# ============================================================================
# These settings enable Azure AD authentication which allows the user's identity
# to be passed through to Microsoft Fabric for data queries.
#
# To configure:
# 1. Register an app in Azure AD (Microsoft Entra ID)
# 2. Configure Redirect URIs (e.g., http://localhost:3000 for local dev)
# 3. Add API permissions: Microsoft Graph (openid, profile, email)
#    and Microsoft Fabric (Item.Read.All, Item.Execute.All)
# 4. Set the values below
# ============================================================================

# NEXT_PUBLIC_AZURE_CLIENT_ID=your-app-registration-client-id
# NEXT_PUBLIC_AZURE_TENANT_ID=your-azure-tenant-id
# NEXT_PUBLIC_AZURE_REDIRECT_URI=http://localhost:3000